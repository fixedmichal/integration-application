import { ConvertToPercentage } from './convert-to-percentage.pipe';

describe('ConvertToPercentagePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToPercentage();
    expect(pipe).toBeTruthy();
  });
});
