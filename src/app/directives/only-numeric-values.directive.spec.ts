import { OnlyNumericValuesDirective } from './only-numeric-values.directive';

describe('OnlyNumericValuesDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyNumericValuesDirective();
    expect(directive).toBeTruthy();
  });
});
